{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://example.com/oscal/batch-practice-stub.schema.json",
    "title": "OSCAL Batch Practice, CIA, and Class Stub Schema",
    "description": "A schema for validating an array of practice, CIA, and class classifications generated by the AI model.",
    "type": "array",
    "items": {
      "type": "object",
      "required": [
        "id",
        "class",
        "practice",
        "effective_on_c",
        "effective_on_i",
        "effective_on_a"
      ],
      "properties": {
        "id": {
            "description": "The control id, echoed verbatim from the input so results map back to the correct control.",
            "type": "string"
        },
        "class": {
            "description": "The standard NIST class of the control.",
            "type": "string",
            "enum": ["Technical", "Operational", "Management"]
        },
        "practice": {
          "description": "The assigned practice category.",
          "type": "string",
          "enum": [
            "GOV", "ISGA", "DOCS", "RISK", "REAL", "PERF", "LAW", "ARCH", "SENS",
            "REA", "NOT", "ASST", "TEST", "PERS", "BES", "DEV", "CON", "DLS",
            "GEB", "BER", "DET", "SYS", "APP"
          ]
        },
        "effective_on_c": {
            "description": "The control's effectiveness on Confidentiality.",
            "type": "boolean"
        },
        "effective_on_i": {
            "description": "The control's effectiveness on Integrity.",
            "type": "boolean"
        },
        "effective_on_a": {
            "description": "The control's effectiveness on Availability.",
            "type": "boolean"
        }
      },
      "additionalProperties": false
    }
}